declare module 'JSONStream'

// {
//   import { Stream } from 'stream';

//   export function parse(path?: any): Stream;
//   export function stringify(open?: string, sep?: string, close?: string): Stream;
//   export function stringifyObject(open?: string, sep?: string, close?: string): Stream;
// }
